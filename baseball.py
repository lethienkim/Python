class baseball():
    
    def single(self,x):
        #local variable
        y=0
        y=x*1
        return(y)
    def double(self,x):
        y=0
        y=x*2
        return(y)
    def triple(self,x):
        y=0
        y=x*3
        return(y)
    def homerun(self,x):
        y=0
        y=x*4
        return(y)
